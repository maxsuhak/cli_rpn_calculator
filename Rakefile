desc 'Use the CLIRPNCalculator'
task :console do
  require_relative './bin/boot'

  calculator = CLIRPNCalculator.new({ input: 'console', output: 'console' })
  calculator.welcome_message
  calculator.start
end
